# Claude Code compatibility baseline

This file is the bookmark and the checklist for the periodic Claude Code
changelog review (`/check-claude-update`). The check reads the baseline,
examines only changelog entries newer than it, cross-references them against
the surfaces below, and updates the baseline when done.

## Baseline

```
last-checked-version: 2.1.252
last-checked-date: 2026-09-01
```

## Version floors

The minimum Claude Code versions the plugins rely on, each detected or
declared at its own site. An update entry that moves one of these is a
breaking change by definition.

| Floor | Feature | Detected / declared at |
|---------|---------|------------------------|
| 2.1.139 | `/goal` guard for phase sessions | `run-workflow.sh` runtime check, declared NOTE fallback |
| 2.1.170 | `fable` model alias | `run-workflow.sh` (repair hardcodes it; phases may pin it) |
| 2.1.224 | cross-session `SendMessage` in the CLI | `refs/foreman.md` → *Channel floors* (single source for messaging floors) — conditional on `Channel: relayed`: an in-chat workflow sends nothing and needs no floor |

## Plugin surfaces

The mechanisms of Claude Code this repo's plugins depend on. An update that
changes any of these can break the plugins without a single line of plugin
code changing. Every new changelog entry is judged against this list.

### phased-workflow

1. **Skill frontmatter** — `description`, `disable-model-invocation`,
   `allowed-tools`, `argument-hint` in every `skills/*/SKILL.md`. A parsing or
   semantics change can make a skill model-invocable when it must not be, or
   strip its tool permissions.
2. **Headless plugin-skill invocation** — `claude -p "/phased-workflow:<skill>"`
   in `scripts/run-workflow.sh` and `scripts/agent-session.sh`. The autonomous
   run depends on headless sessions resolving plugin slash commands.
3. **CLI flags** — `--model fable|opus`, `--permission-mode auto` in the same
   scripts. A renamed flag or a removed model alias fails the run at launch.
   `fable` landed in 2.1.170 and sets the floor for autonomous runs; the repair
   session hardcodes it, a phase may pin it via `Model:`.
4. **Settings inheritance** — the scripts copy `.claude/settings.local.json`
   into the plan checkout so sub-sessions inherit permissions. A change in
   where/how sessions read settings stalls the autonomous run on permission
   prompts.
5. **Agent frontmatter** — `name`, `tools`, `model: opus` in `agents/*.md`.
   Model restriction or alias changes silently downgrade the verifier/judge
   agents (since 2.1.223 a warning is shown).
6. **Plugin loading** — manifest parsing (`.claude-plugin/plugin.json`),
   symlinked development installs, plugin cache behavior, skills path
   resolution.
7. **Auto permission mode semantics** — sub-sessions run `--permission-mode
   auto`; changes to the auto-mode classifier or its consecutive-block limits
   alter how far an unattended phase can get.
8. **git worktree assumptions** — the plan checkout is a plugin-managed git
   worktree launched as a *normal* session (cwd inside it). It does not use the
   harness's native worktree isolation; changes to native isolation are
   irrelevant, changes to how sessions treat worktree checkouts are not.
9. **Host toolchain** — `bash` for the launchers and `python3` for
   `scripts/next-phase.py`, which every skill shells out to. Both are assumed
   present under those exact names. A change in which shell Claude Code runs
   Bash tool commands through breaks plan resolution on the interactive path
   too, not only the autonomous one. This is what confines the plugin to
   macOS/Linux (and WSL).

10. **`SendUserFile`'s `display` parameter** — the closing reports are handed
    over with `display: render` (`refs/foreman.md` → *The reporting register*).
    The page lives outside the project folder by design, and a client left to
    choose attaches such a file as a download card instead of rendering it — the
    field defect the explicit parameter answers. A rename or a semantics change
    silently costs every closing report its hypertext form.
11. **Session transcript JSONL** — `scripts/wfdash/core.py` walks the session
    transcripts incrementally by byte offset, reading turns, models, output
    tokens, cache reads, thinking and tool calls out of their JSONL records. A
    schema or path change breaks the dashboard's cost and activity reading (the
    dashboard only — no skill depends on it).
12. **`claude agents --json`** — the live session list, read by
    `scripts/wfdash/core.py` through `subprocess.run` and cached 3 s. A renamed
    subcommand or a changed shape leaves the dashboard with no active agents.
13. **`~/.claude/tasks/<sessionId>/N.json`** — the agents' own todo lists, shown
    by the dashboard as an explicit estimate. Undocumented by definition; a
    change degrades that one panel.
14. **`~/.claude/sessions/<pid>.json`** — its `cwd` and `sessionId`, read by
    `scripts/wfdash/inbox.py` to recognise the chat that opened the dashboard
    and the live sessions on this repository. Read-only since the dashboard
    stopped writing turns itself, so the `peerToken` and the unix socket beside
    them are no longer touched; a change leaves the page unable to name the chat
    that will drain the queue.

Surfaces 11–14 belong to the dashboard, which is an OPTIONAL surface: a break in
any of them costs the page, never a workflow — `refs/board.md` → *The dashboard,
where it exists*.

### genropy-worktree

Shell-only (`bin/`, `install.sh`); exposed to git and shell behavior, not to
Claude Code internals. Low sensitivity.

## Classification

Each relevant entry gets one verdict:

- **BREAKING** — a listed surface changes incompatibly; plugin needs a fix.
- **BEHAVIOR** — semantics shift on a listed surface; verify, maybe adapt.
- **POSITIVE** — fixes or improves a listed surface; nothing to do.
- **IRRELEVANT** — does not touch any listed surface.

When in doubt between IRRELEVANT and anything else, flag it: a false alarm
costs one human glance, a false silence does not.

## Review log

| Date | Window | Result |
|------|--------|--------|
| 2026-09-01 | 2.1.251 → 2.1.252 | **Two behavior shifts to verify, both on paths this repo reaches by hand rather than through a documented API.** BEHAVIOR (surfaces 9 + 13): "Fixed Bash commands failing with `task output swap refused (tasks dir moved or linked)` on some Macs" — the fix itself is pure gain on surface 9, and on this host it is not hypothetical: a Bash tool that refuses is a phase that cannot shell out to `next-phase.py` at all, so plan resolution dies on the interactive path too. What earns the flag is the mechanism it relaxes — a guard on the tasks directory being *moved or linked* — because `scripts/wfdash/core.py:48` hardcodes `TASKS = HOME / '.claude' / 'tasks'`. Wherever the harness now tolerates a relocated or symlinked tasks dir, that constant points at the wrong place and the todo panel empties without saying why. Surface 13 is dashboard-only and undocumented by definition, so this costs the page and never a run (`refs/board.md` → *The dashboard, where it exists*); the check is simply that the panel still fills the next time the dashboard is open. BEHAVIOR (surface 4): "Fixed 'always allow' not saving in a project that has no `.claude/settings.local.json` yet" — both launchers copy the file only when the source repo already has one (`run-workflow.sh:93`, `agent-session.sh:73` guard the `cp` with `[ -f ]`), so a repo without one leaves the plan checkout without one too: precisely the case this fix changes. A sub-session that grants "always allow" inside the checkout now *writes* a `.claude/settings.local.json` there, where before the grant was silently dropped. Two consequences to watch. The grant still dies with the checkout, so the autonomous run gains no permission memory across phases — the copied-in file remains the only inheritance path, and the fix does not make one unnecessary. And the new file is untracked in a worktree whose clean-tree invariant every phase session checks at its start: on this machine it is covered by `~/.gitignore_global:35`, NOT by anything in the repo, so a run on a host without that global ignore can find a phase start refusing a tree the harness dirtied on its own. Verify on the next autonomous run in a repo carrying no committed settings.local.json. POSITIVE (surface 2): "Fixed background task notifications with very large failure output (for example git errors on a full disk) making the conversation exceed the API request size limit" — `/run-workflow` launches the runner as a background task and the foreman session watches it for the whole run, so a phase dying on a fat git error was one of the shapes that could blow the request limit out from under the watching chat. IRRELEVANT: 1 (Remote Control sessions hosted by Claude Desktop or VS Code stalling on a degraded claude.ai connection — the launchers use local headless `claude -p` sessions and no Remote Control path). |
| 2026-08-30 | 2.1.248 backfill (baseline stays 2.1.251) | **Nothing newer than the baseline; 2.1.248's entries, recorded as absent in the row below, are now published and reviewed here.** The changelog's head is unchanged (2.1.251 → 2.1.250 → **2.1.248** → 2.1.247 → 2.1.246); 2.1.249 is still absent. 2.1.248 sat *inside* the window the row below declared reviewed, so leaving it unread would be a false silence on 49 entries, not a deferral. BEHAVIOR (low, surfaces 3, 4): "Added `--restricted` (or `CLAUDE_CODE_RESTRICTED=1`): removes the built-in tools that run commands or code and `WebFetch` (unless named in `--tools`), keeps file tools inside the working directory, refuses `bypassPermissions`, and **ignores user, project and local settings files**" — a single environment variable that, if ever exported on a host running an autonomous plan, kills every phase at its first Bash call and silently voids the `.claude/settings.local.json` both launchers copy into the plan checkout (`agent-session.sh:73-74`, `run-workflow.sh:89-90`), which is the whole mechanism surface 4 exists for. Opt-in and not set on this host (checked: no `CLAUDE_CODE_RESTRICTED` in the environment, no `--restricted` anywhere in the repo), but unlike the managed-settings gates tracked since 2026-08-27 this one needs no managed host and no admin — one inherited env var does it, and the failure presents as a phase that cannot act rather than as a launch refusal. BEHAVIOR (low, surface 8): "Fixed a backgrounded worktree session losing its checkout: the background session now holds the worktree's lock while it runs, so cleanup and `git worktree remove` leave it alone" — the fourth worktree entry on record and the first whose *fix* creates the new failure mode here: `finalize-workflow/SKILL.md:126` prescribes a plain `git worktree remove .claude/worktrees/<slug>`, which a `git worktree lock` refuses outright. Scoped to "backgrounded worktree session", and no launcher creates one — both run foreground `claude -p` (re-verified: no `--bg` and no `claude agents` spawn anywhere in `plugins/wf/scripts/`; the only `run_in_background` is the dashboard's own server process, `dashboard/SKILL.md:78`, a Bash task and not a Claude session) — so this is a near-miss like 2.1.251's edit fix, 2.1.246's retention sweep and 2.1.239's `.git/config.worktree`. If it ever fires, it is visible and recoverable (`git worktree unlock`, or `--force`), not silent. POSITIVE, surfaces 2, 3: "Fixed being sent to the login screen when another Claude Code process held the token refresh lock while the session token had expired; the request now fails with a retryable error instead" — a long run is *exactly* concurrent processes on one account (the launcher, its phase sessions, an interactive chat alongside), and a headless `claude -p` sent to a login screen it cannot answer fails the phase for a reason unrelated to its work. POSITIVE, surface 12: `claude agents` no longer crashing on launch on a malformed PR-status cache entry, and no longer skipping the workspace trust prompt when `CI` is set — `scripts/wfdash/core.py:342` shells `claude agents --json`, and a crashing subcommand leaves the dashboard with no active agents at all. POSITIVE, surface 2: "Improved the Workflow tool's prompt footprint: its description is now about 1k tokens instead of 5.7k" — no skill here calls that tool, but every 25-turn phase session paid the 5.7k in its system prompt regardless; ~4.7k tokens back per session, per turn, on the cached prefix. POSITIVE, surface 2: "Fixed a prompt-cache miss (and lost extended-thinking context) roughly once an hour in long sessions, caused by tool definitions being re-rendered after an OAuth token refresh" — a run that spans hours took a full re-read per hour per session; also relevant to what `scripts/wfdash/core.py` reports as cache reads (surface 11). POSITIVE, surface 5: cross-session messaging (`SendMessage`/`ListAgents`) now available between same-machine sessions on Bedrock, Vertex and Foundry and when telemetry is disabled — widens where the foreman relay can exist at all (`refs/foreman.md` → *Channel floors*); moot on this host (default `api.anthropic.com`, telemetry on), but if the relay is ever wanted on such a host the floor for it is 2.1.248. POSITIVE, surface 5: "Improved cross-session messaging: falls back to a private per-user `/tmp` directory when the default one can't be used, and the notice and `/status` name the directory to fix" — this session's own socket is `/tmp/cc-socks/…`, so the shared-`/tmp` failure mode is the live configuration. POSITIVE, surface 5: `experimental.cacheTtl` (`"5m"`/`"1h"`) is now settable in agent frontmatter — additive, no agent here sets it (checked), but it is the one knob that could pin a TTL for `phase-verifier`/`ui-judge`/`report-judge` without touching a global setting. Checked and cleared: "Fixed an invalid `crossSessionInbound` value being silently ignored: it now warns and **holds** cross-session messages (user settings) or refuses them (managed settings) until fixed" (surface 5) — a typo in that key used to be ignored (i.e. inbound stayed open) and now closes the channel, which is a new way for the foreman relay to be dead while both ends believe it live; the key is set nowhere (checked `~/.claude/settings.json` and every file under `.claude/`), so it cannot fire here; "Changed `SendMessage` from a subagent to another session: the result now notes that any reply is delivered to the parent session's conversation, not to the subagent" (surface 5) — none of `phase-verifier`/`ui-judge`/`report-judge` carry `SendMessage` in their `tools` (Read/Grep/Glob/Bash only), and every foreman message is sent by the phase session itself; "Fixed background sessions waiting silently when a `PermissionRequest` or `PreToolUse` hook prints an invalid answer" and "Fixed hooks silently treating a stdout `{…}` object that isn't valid JSON as plain text" (surfaces 4, 6) — no hooks configured anywhere in this repo, as established repeatedly; "Fixed `claude agents` and `claude rm` refusing to delete a session ('has commits that are not pushed anywhere') when its worktree branch was already merged" (surface 8) — that is the harness's own background-session worktrees, not the plugin-managed plan checkout, which `finalize-workflow` removes with plain git; "Fixed Claude Desktop and Cowork sessions disappearing after 30 days" plus the new `desktopSessionCleanupPeriodDays` (surface 11) — the transcripts `scripts/wfdash/core.py` walks are CLI ones, and the change only *keeps* sessions longer, never fewer; "Improved managed settings: client-side timeout, MCP startup-mode, and stream-watchdog env vars no longer trigger the settings-approval prompt" and the new server-managed-settings `/doctor`/`/status` diagnostics (surface 4) — no managed settings on this host; the MCP `headersHelper` 401 fix and `/mcp` connector-scope fix (surface 2) — no `.mcp.json` and no inline MCP anywhere in the repo. 32 of 49 entries irrelevant (Windows `claude agents` keyboard, Console sign-in fallback in `/login`, `/model` names rendering as code, four agent-view/`claude agents` session-resume row fixes, `/login` gateway hang and gateway model discovery under `apiKeyHelper`, `claude logs` leaving mouse tracking on, trust-dialog emoji truncation, permission-mode indicator behind the Ctrl-C hint, `/ultrareview` credential-file upload hardening and its `<PR#>` access precheck, two Remote Control fixes, cloud-session startup credentials, startup-warning column offset, @-mention IME matching, `/usage-credits` for AWS-Marketplace Enterprise and the rate-limit messages naming it, VSCode "No conversation found", PR-badge re-check throttle, shift+enter in the agent-view dispatch input, `/loop` dynamic mode on third-party providers, Anthropic telemetry log level, Linux user-namespace trust narrowing, `self-hosted-runner --client-label`, `/web-setup` `workflow`-scope warning, `ScheduleWakeup` definition drift across `--resume`). |
| 2026-08-30 | 2.1.247 → 2.1.251 | **Four behavior shifts to verify, one of them on an effort level the plugin actually launches and one on a boundary `foreman.md` states as field-tested.** (2.1.248 and 2.1.249 do not appear in the changelog; the window is 2.1.247 → 2.1.250 → 2.1.251.) BEHAVIOR (2.1.251, surfaces 3, 5): "Fixed Opus 5 requests failing with 'effort … is not supported when thinking is disabled' when effort was xhigh/max and thinking was turned off; effort is now sent as `high` in that case" — this plugin launches exactly that combination: the opus leg of the repair fallback runs `--model opus --effort max` (`run-workflow.sh:626-628`), the fable leg `--model fable --effort max` (`:608-610`), and `write-workflow-autonomous.md:146` prescribes `opus` at `xhigh` for every final review phase. Both directions matter. Before this release, on a host with thinking off, an opus/max repair session would have *failed the request outright* — and the launcher's fallback chain has no third leg, so a failed fable repair falling back to a failing opus repair leaves the phase `[!]` with `Repair attempted:` never written and the run stopped. After it, the same session runs at `high`, an effort nobody declared, and nothing in the launcher or the plan reads back which effort answered. On THIS host the condition does not fire: no thinking-disabling setting exists in `.claude/settings.local.json` or `~/.claude/settings.json` (checked — the only grep hit is the literal text of an allow-rule written by a previous run of this very skill), and no script sets `MAX_THINKING_TOKENS`. Verify before trusting a `max`/`xhigh` phase on any host where thinking has been turned off in `/config` or by a host app. BEHAVIOR (2.1.251, surface 5): "Fixed replying to a message Claude Desktop delivered from another session: `SendMessage` to that session id now delivers through Claude Desktop instead of failing with 'not reachable'" — `refs/foreman.md:294` states flatly that "`SendMessage` does not resolve desktop sessions or their `session_id`", and `:135-136` states as field-tested on 2.1.226 that "CLI and desktop are separate worlds". This release carves a case out of both: where the desktop delivered the message first, a reply by session id now routes back through Desktop. It does not falsify the general claim — an unprompted CLI→desktop `SendMessage` is still unreachable — but this is the second release in a row (after 2.1.238's Remote Control peers) to move that boundary, and the whole foreman protocol reads reachability off it. Re-test the branch declaration (`:113-114`) from a desktop-hosted chat; if the reply path now works, the floor for it is 2.1.251, not 2.1.224. BEHAVIOR (low, 2.1.251, surfaces 7, 9): "Fixed Bash permission checks auto-approving commands that assign an arithmetic expression to an integer shell variable (e.g. `OPTIND=1/0`, `RANDOM=2+2`); these now prompt for approval" — same family as the 2.1.246 dangling-operator entry: every `--permission-mode auto` sub-session issues model-authored Bash, and a shape that was auto-approved now raises a prompt with nobody at the keyboard. The launchers' own arithmetic (`MAX_SESSIONS=$((REMAINING * 2))`, `SESSIONS=$((SESSIONS + 1))`, `BUDGET=$((BUDGET * 2))`, `run-workflow.sh:310,314,434,548,576,668`) runs inside bash directly and never passes the Bash tool's checker, and the one arithmetic any skill prescribes is an *expansion* in an argument, `tail -n +"$((SEEN + 1))"` (`run-workflow/SKILL.md:66`), not an assignment to an integer variable — so no known emission matches the new shape. Unverified whether auto mode's classifier resolves such a prompt on its own, the same open question standing since 2.1.243. BEHAVIOR (low, 2.1.251, surfaces 4, 7): "Fixed managed-settings `disableAutoMode` arriving mid-session not moving an already-running auto-mode session back to default mode" — a new mechanism for demoting a *running* `--permission-mode auto` session to default mode mid-flight, which for an unattended phase means it stops being able to act and stalls on prompts partway through work already begun. Cannot fire on this host (no `/Library/Application Support/ClaudeCode/managed-settings.json`, checked again this run), but on a managed host it turns an org policy change into a mid-run stall rather than a launch-time refusal — the sharper sibling of the launch-time gate noted on 2026-08-27. UNASSESSABLE (2.1.250): the single line "Bug fixes and reliability improvements", no mechanism named — same shape as 2.1.240/241, not flagged but not cleared either; if a surface regression appears on 2.1.250, that line is where it hides. POSITIVE, surface 6 (a real prior exposure, now closed): "Fixed background sessions occasionally starting without any plugin skills (and staying that way) when another Claude Code process was refreshing the plugin marketplace at the same moment" — a session that starts with no plugin skills cannot resolve `/wf:execute-phase-agent`, which is precisely how `agent-session.sh:101` and `run-workflow.sh:480` launch every phase; concurrent runs against the same installation (two plans, or a chat opening while a run is live) are exactly the race named. Scoped to "background sessions" and both launchers use foreground `claude -p`, so likely a near-miss rather than a hit, but the failure mode it describes is silent and total. POSITIVE, surface 8: "Fixed background sessions and their subagents being unable to edit files inside a git worktree they created with `git worktree add`" — this plugin's exact shape (`agent-session.sh:71`, `run-workflow.sh:91` create the plan checkout, sessions then run with cwd inside it); same "background sessions" scoping caveat, and the third worktree near-miss on record after 2.1.246's retention sweep and 2.1.239's `.git/config.worktree`. POSITIVE, surface 5: "Changed `CLAUDE_CODE_SUBAGENT_MODEL` to set the default subagent model rather than override everything: an agent definition's `model:` and an explicit per-spawn model now take precedence over it" — directly protective of what surface 5 exists to catch, since `phase-verifier`/`ui-judge`/`report-judge` carry `model: opus` in frontmatter and that declaration now outranks the env var (not set in this environment either way, checked). POSITIVE, surface 2: "Fixed conversations getting stuck on 'text content blocks must be non-empty' errors after a turn where the model produced only thinking" — a wedged headless phase session burns its wall-clock budget for no reason tied to its own work; also "Improved retry when the model's tool call is malformed: the broken output is now dropped from the retry context". POSITIVE, surfaces 2, 7: "Fixed the one-time 'make auto mode your default' offer appearing in unattended sessions, where a stray keypress could accept it unread" — unattended is this plugin's entire autonomous path. POSITIVE, surface 11: "Fixed session transcripts being silently overwritten when a directory change relocated a session onto an existing same-ID transcript" — `scripts/wfdash/core.py` walks those transcripts incrementally by byte offset, and an overwritten file is the one case that corrupts an offset-based reader rather than merely truncating it. POSITIVE, low: `claude --bg --model fable` no longer stopping for usage credits while the interactive session on the same account still has Fable allowance (surface 3 — no launcher passes `--bg`, but it is the fable-credits path the fable→opus fallback exists for). Checked and cleared: "Fixed plugin commands declared in a marketplace entry being able to point outside the plugin directory; such paths are now rejected with a path-traversal error" (surface 6) — `.claude-plugin/marketplace.json` declares one plugin as a plain `./plugins/wf` path source with no `commands` field at all, so nothing in this repo was ever in that blast radius; "Fixed Grep and Glob not applying `Read(...)` deny rules to files reached through a symlinked search path" (surface 4) — `.claude/settings.local.json` carries only `permissions.allow`, no `deny` rules anywhere in the repo; "Changed project-level `.claude/settings.json` `env` to no longer set `CLAUDE_CONFIG_DIR`, `CLAUDE_CODE_TMPDIR`, or `TMPDIR`/`TMP`/`TEMP`" (surface 4) — there is no `.claude/settings.json` in this repo and no `env` block in any committed settings file, though note this as a precedent for project-scope settings keys silently ceasing to apply, since both launchers copy `settings.local.json` into the plan checkout; `ANTHROPIC_CUSTOM_HEADERS` from managed or project settings now requiring approval (surface 4) — not set anywhere; the managed-settings sandbox-TLS/proxy/credential approval gate and the changed-settings-only approval dialog (surface 4) — no managed settings on this host; "Fixed file tools (Read, Write, Edit) following a symlink swapped inside the working directory after the permission check" (surfaces 8, 9) — no symlinks under `.claude/` (checked), and the fix targets a TOCTOU swap, not ordinary symlink following, so a symlinked dev install is not in scope; "Changed Claude in Chrome so browser actions always go through Claude Code's permission checks" (surface 7) — the only browser tools this plugin declares are `mcp__Claude_Browser__preview_start`/`navigate` in `dashboard/SKILL.md:4`, the in-app Browser pane, a different mechanism from the Chrome extension, and the dashboard is an optional surface besides; the new `PreModelSwitch`/`PostModelSwitch` hook events and `SessionStart` resume staleness (surfaces 4, 6) — no hooks configured anywhere in this repo, as established repeatedly; "Fixed the Workflow tool reading a `scriptPath` outside what the session may read" (surface 2) — no skill in this plugin calls the Workflow tool; `attach`/`logs`/`stop`/`respawn`/`rm` added to `claude --help` (surface 12) — additive subcommands, `claude agents --json` is neither renamed nor reshaped by this entry, and the installed CLI is 2.1.237 so `scripts/wfdash/core.py:342` could not be exercised against 2.1.251 on this host; "Changed the default commit trailer to `Co-Authored-By: Claude Code` when the active model isn't a recognized Claude model" — the launchers always pass `--model fable|opus`, both recognized, so the condition never holds for a phase commit; `/effort` saving a default per model — every launch passes an explicit `--effort`, so a stored default is never consulted; "Improved framing of messages from your own subagents: Claude is told the sender is a worker inside this session" and "Fixed background subagents being unable to reply to a message from an unnamed sibling or parent agent" (surface 5) — the plugin consumes `phase-verifier`/`ui-judge`/`report-judge` findings as Agent-tool return values (`execute-phase-agent/SKILL.md:79`, `repair-phase/SKILL.md:65`), never as subagent-sent messages, and the foreman relay's own traffic is cross-session between unrelated sessions, whose framing is untouched; `--input-format stream-json` client-injected tool calls, agent-team teammate answers, and the Bash sandbox output-file hardening (surfaces 7, 9 — no `sandbox` key in any settings file here, and the launchers' `tee` to `$PLAN_DIR/log/*.txt` happens outside the Bash tool) — none of those code paths are used. 45 of 72 combined entries irrelevant (spend-limit bar and `rate_limits.spend_limit`, `/cost` prompt-cache line, Remote Control subagent streaming and its policy/`/mcp reconnect`/VSCode-pill fixes, TUI parallel-subagent lag, Opus 1M tip, gateway stored-profile and re-approval fixes, cloud-session model/proxy/GitHub-advice fixes, tmux-over-SSH selection, SDK MCP handshake timeout, self-hosted-runner stuck Bash processes, `/usage-credits` at \$0, GitLab `--worktree --tmux` fetch, Ctrl+G in `emacs -nw`, `additionalDirectories` null byte, MCP menu copy shortcut and name sanitization, italics in GNU screen, `claude mcp add` help text, `ultrareview` 30-minute wait, backgrounded Vertex/Bedrock gateway env, disabled `/bug`/`/share` wording, CPU and install-size improvements, `/schedule` MCP message, subagent-panel prompt placeholder, Bedrock inference-profile discovery, `/radio` on third-party providers, Enterprise default model, analytics-before-sign-in, footer PR badge, install-suggestion Enter guard, six removed syntax highlighters, VSCode sign-in button). |
| 2026-08-27 | 2.1.246 → 2.1.247 | **Three behavior shifts to verify, one of them a real semantics change on the model the verifier agents declare.** BEHAVIOR (surface 5): "Fixed sub-agents dying on a first-call model 404: they now use the session's fallback model chain, and the error returned to the parent includes the error type, status, request id, and model" — all three agents declare `model: opus` in frontmatter (verified: `plugins/wf/agents/phase-verifier.md:5`, `ui-judge.md:5`, `report-judge.md:5`), and surface 5 exists precisely to catch a silent downgrade of the verifier/judge agents. The fix direction is positive — a 404 on the declared model no longer kills the verification outright, and the parent now gets a typed error with the model name in it — but the observable outcome on a 404 changes from "the subagent died, loudly" to "the subagent ran on whatever the session's fallback chain resolved to", which for a `fable` phase session means a verification possibly produced by a model nobody declared. Nothing in `execute-phase-agent/SKILL.md:79` or `repair-phase/SKILL.md:65` reads which model answered; the findings are consumed as prose either way. Verify, if a 404 is ever observed, that the phase's verdict came from opus before trusting it. BEHAVIOR (low, surface 4): "Added a tip on Bash permission prompts pointing to auto mode, with a one-keystroke 'Yes, and switch to auto mode' option" — both launchers copy `.claude/settings.local.json` into the plan checkout so sub-sessions inherit `permissions.allow` (`agent-session.sh:73-74`, `run-workflow.sh:89-90`), and those rules are what an interactive "don't ask again" writes. A one-keystroke option that resolves the prompt by flipping the session to auto mode instead of recording a rule means an interactive answer can now leave the allow-list narrower than the same answer used to, and the narrowing surfaces later as an unattended phase stalling on a prompt with nobody in front of it — the same mechanism this file has tracked since 2.1.235. Verify after the next interactive run that the rules the autonomous path relies on are still recorded. BEHAVIOR (low, surface 5): "Fixed `/rename` silently confirming when the session registry could not be updated; it now says other sessions may still show the old name" — the whole foreman protocol addresses sessions by exact title through `list_sessions`/`ListAgents` (`refs/foreman.md:114-120`), set via `set_session_title`. This entry establishes that the session-registry write can fail and leave peers resolving a stale name, which is the same "dead channel that must be declared, not discovered" family as the still-open 2.1.239 item (titles disappearing after >64 KB). Whether `set_session_title` shares `/rename`'s registry-write path is unverified; if it does, a title-addressed foreman lookup can miss while both ends believe the channel is live. POSITIVE, surface 5: "Fixed a hook or background agent that printed megabytes of error output being able to overflow the conversation and wedge the session on 'Prompt is too long'" — no hooks are configured anywhere in this repo, so only the background-agent half applies, and it applies squarely: `phase-verifier`/`ui-judge`/`report-judge` spawn in the background by default since 2.1.232, and a verbose failure in one of them could wedge the phase session that was waiting on it. POSITIVE, surfaces 5 and 9: "Fixed unbounded memory growth when a hook's or background task's output file could not be written; the file now notes where output was lost" — same background-subagent path, and the launchers' own `tee` to `$PLAN_DIR/log/*.txt` is the shape where a lost-output note beats silence. POSITIVE, surface 6 (checked, bug never applied): "Fixed a version-less marketplace plugin's live cache directory being deleted and recreated on a second-scope install, which could disrupt a running session using it" — this names exactly the surface-6 exposure that would bite an autonomous run mid-flight, but `plugins/wf/.claude-plugin/plugin.json` declares `"version": "6.24.0"`, so the plugin is not version-less and was never in the blast radius. Checked and cleared: the Bash sandbox's after-command cleanup deleting a dotfile-managed `~/.claude/settings.json` symlink under nix/home-manager/stow (surfaces 4, 9) — on this host that path is a plain regular file (`-rw-------`, not a symlink), no dotfile manager repoints it; `/compact` and "Summarize from here" summarizing under the default system prompt in `--agent` sessions (surface 2) — neither launcher passes `--agent` (no match in `plugins/wf/scripts/`); organization sign-in enforcement now exiting at start when managed settings cannot be read (surface 4) — no `/Library/Application Support/ClaudeCode/managed-settings.json` on this host, so the gate never fires, though note it as a new way for the CLI to refuse to start at all, which would fail every autonomous run at launch on a managed host; Sonnet 5's auto-compact window moving to the full 1M context (surface 3) — the scripts always pass an explicit `--model fable|opus`, never sonnet; the `claude agents` row stuck on "opening…" after its terminal host died, and foreground shell commands logging an internal error or `[exited with code -1]` when they finish in background sessions (surfaces 2, 5, 9) — both launchers run `claude -p` in the foreground and never create background sessions via `claude agents`; cross-session peer messages now collapsing to a one-line `Message from @<sender>: <first line>` preview with Ctrl+O to expand (surface 5) — every message the protocol sends is a single line by template (`refs/foreman.md:131-145`), and the collapse is terminal display, not the `<system-reminder>` the receiving model reads, so nothing is truncated in either layer; plugin-marketplace name hardening against control and invisible characters (surface 6) — the plugin name is `wf`, plain ASCII; MCP connection failures now reported to Claude on Bedrock/Vertex/Foundry and telemetry-disabled sessions — none of those three conditions apply here and no MCP is configured in the repo; the new `SendFeedback` tool and `feedbackDrafts` setting (surface 1) — additive, no skill's `allowed-tools` lists it and a headless `-p` session has no `/feedback` to send from; the arrow-key + Enter off-by-one fix in the history/`/config`/`/mcp`/`/skills`/`/model` pickers (surfaces 1, 3) — interactive pickers only, skills are invoked by name and models by `--model`. 27 of 33 entries irrelevant or cleared (spinner-tip overrides, `/claude-api cost-optimize` and its Admin API coverage, Cyrillic Ctrl shortcuts in kitty terminals, split mouse-report escape sequences, `/terminal-setup` Zed keymap merge, `/install-github-app` over SSH, Remote Control working-tree diff, self-hosted-runner premature-notification fix, first-run gateway connectivity error, two cloud-session fixes, terminal-hyperlink plain-text rendering, PR-badge re-check throttle, analytics-off-from-startup, gateway sign-in identification). |
| 2026-08-26 | 2.1.245 → 2.1.246 | **Three low-confidence behavior shifts to verify; one real find on surface 8 that turned out already fixed.** BEHAVIOR (low, surface 5): "Improved subagent results: a subagent that stops at its `maxTurns` limit now returns its output marked as partial, with a hint to continue it via `SendMessage`, instead of appearing finished" — `phase-verifier`/`ui-judge`/`report-judge` (none pin a `maxTurns`) and every other Agent-tool subagent the skills spawn were previously at risk of a truncated finding silently "appearing finished" to the calling skill; the fix direction is entirely positive, but the marker text now added to a truncated agent's returned string is a shape change nothing here has seen — unverified whether it could confuse a skill reading that string as freeform review prose (`execute-phase-agent/SKILL.md:79`, `repair-phase/SKILL.md:65`). BEHAVIOR (low, surface 2): "Improved non-interactive sessions (`-p`, SDK, cloud sessions) to automatically continue a response cut off mid-stream by a server error, connection loss, or stall instead of ending with an error" — every phase/repair session is a headless `claude -p` launch (`run-workflow.sh`), and the launcher's failure-handling paths (the fable→opus repair fallback, the parent Monitor's non-zero-exit check) were built assuming a stream cutoff ends the turn with an error; it now retries silently instead, which should mean fewer spurious phase failures but also means a script watching for that error as a signal gets it less often. Net direction positive, not verified either way. BEHAVIOR (low, surfaces 7, 9): "Fixed Bash permission checks to always require approval for malformed commands with a dangling `&&` or `||` operator" — every `--permission-mode auto` sub-session issues model-authored Bash, and this adds a case that now needs approval where before it may not have; whether auto mode's classifier resolves it on its own or the run stalls on a live prompt is the same open question as the 2.1.243 sandboxed-network-host entry, unverified here since no script or skill emits a command with a dangling operator today. POSITIVE, surface 8 (checked and found the risk already closed): "Fixed the background retention sweep removing git worktrees under `.claude/worktrees/` that you created yourself when an old background-session record pointed at them" — this plugin's plan checkouts live at exactly `$REPO_ROOT/.claude/worktrees/$PLAN_SLUG` (`run-workflow.sh:89`, `agent-session.sh:69`), so this names a real prior exposure (Claude Code's own background-session cleanup could delete an active plan's worktree out from under it); as of 2.1.246 it no longer does, nothing to do now but worth having on record as a surface-8 near-miss. POSITIVE, surface 6: "Fixed `/reload-plugins` reporting 0 skills for plugins that define skills under `skills/*/SKILL.md`" — this is this plugin's exact layout (`plugins/wf/skills/*/SKILL.md`, confirmed no `name:` frontmatter field on any of them, so nothing else in that bug's blast radius applies); interactive dev-install reload of this plugin was undercounting its own skills until this release. POSITIVE, low relevance: background sessions no longer failing to open after a 45s timeout on a deleted starting directory/slept machine/slow host, and no longer failing with EACCES during a concurrent npm reinstall (surfaces 2, 5); Windows/macOS headless sessions no longer leaving stale entries in `~/.claude/sessions` from unclean exits (surface 9); Bash tool latency improved on bash shells via snapshot functions without a base64 subshell (surface 9, no behavior change). Checked and cleared: the new startup warning for Bash allow-rule wildcards before the subcommand (surface 4) — no rule anywhere in this repo's `.claude/settings.local.json` or any committed settings file has that shape (checked, only trailing wildcards like `Bash(cd ... *)`); the Workflow-tool dynamic-workflow-restart confirmation prompt (surface 2) — no skill in this plugin calls the `Workflow` tool's `agent()`/`pipeline()`/`parallel()`; `--setting-sources` filesystem-sandbox fix (surfaces 4, 9) — no script passes that flag; plugin-install BOM fix (surface 6) — `plugins/wf/.claude-plugin/plugin.json` opens `7b 0a 20` (`{`, no BOM); `claude plugin update` bare-name fix and `known_marketplaces.json` corruption fix (surface 6) — both are human-run CLI paths, no script calls either; hook error messages showing a literal `${CLAUDE_PLUGIN_ROOT}` (surface 6) — no hooks configured anywhere in this repo; `apiKeyHelper` JWT-refresh fix (surface 3) — not configured; `claude install`/`update` deferring managed-settings consent, `/cd` picking up project settings live, `--strict-mcp-config` prompting for unused `.mcp.json` servers, resumed-session 400s on rejected tool blocks — none of these code paths (`claude install`, `/cd`, `--strict-mcp-config`, `--resume`/`--continue`) are used by either launcher. The `/goal` idle check-in cap ("at most three check-ins on long-running background work per goal; your next message allows three more") closes out the thread this file has tracked since 2.1.234/237/238 with an explicit bound — POSITIVE resolution, no longer open. 58 of 68 entries irrelevant (fullscreen/transcript/keybinding/theme/`/stats`/`/fork`/`@`-picker UI fixes, MCP argument/interrupt fixes with no MCP configured, `claude agents` listing fixes, plugin marketplace/OTel/telemetry attribution, Remote Control and cloud-session/`/ultrareview`/`/autofix-pr` fixes, Bedrock/Vertex/Foundry `/code-review` auto-start, self-hosted runner poll retry, `/rename`/theme color fixes). |
| 2026-08-25 | 2.1.241 → 2.1.245 | **One behavior shift to verify, on the sandboxed Bash network prompt auto mode sub-sessions run under.** BEHAVIOR (2.1.243, surfaces 7, 9): "Changed the sandboxed Bash tool prompt to no longer list allowed network hosts, so Claude attempts requests (and you can approve new hosts) instead of assuming unlisted hosts are blocked." Every phase and repair session runs `--permission-mode auto` unattended (`run-workflow.sh:434-442`, `:568-570`, `:586-588`); before this change the model, told the allow-list up front, would not attempt a request to a host outside it and would treat it as blocked without asking. Now it actively attempts unlisted hosts, which means a real approval prompt can appear with nobody at the keyboard — the same "stalls on a prompt with nobody in front of it" failure mode this file flagged for 2.1.235's permission-dialog change. Whether this actually stalls a run turns on whether auto mode's classifier still auto-resolves (approves or denies) that prompt on its own before it reaches a human. Tested directly: a nested `claude -p ... --permission-mode auto` session (mirroring `run-workflow.sh`'s launch) told to curl `https://example.com` — a host on no allow-list — completed in 12s, exit 0, `HTTP_CODE:200`, no prompt encountered. No stall observed on this host/config; the OS-level Bash sandbox may simply not be active in this environment (no `sandbox` key anywhere in `.claude/settings.local.json`), so this clears the concern here but is not a general proof the classifier resolves the prompt under an active sandbox elsewhere. POSITIVE (2.1.243): two auto-mode reliability fixes on surface 7 — auto mode no longer stays unavailable at startup after a cached temporary server-side disable and a later flag-fetch failure, and auto-mode tool calls are no longer denied as "temporarily unavailable" after ~1 minute of waiting when the API was briefly overloaded (both were spurious failure modes an unattended `--permission-mode auto` phase session could hit for no reason tied to its own work); background subagents no longer fail to wake when their last background Bash task completes (surface 5 — `phase-verifier`/`ui-judge`/`report-judge` spawn in the background by default since 2.1.232, and a subagent stuck this way would hang the phase session waiting on it); headless sessions that go silent for 10+ minutes when the API never starts a response now time out after ~3 minutes and retry once instead of hanging (surface 2 — a `claude -p` phase session stuck like this burns its wall-clock budget for no visible reason); sandbox network-violation details are no longer dropped from the Bash tool result when the blocked command still exits 0 (surface 9 — a script depending on `$?` alone to detect a blocked network call would have seen a false success); cross-session messaging no longer silently turns off inside user namespaces and rootless containers, a regression from the 2.1.232 socket-directory hardening (surface 5, low relevance here — this host is neither). POSITIVE (2.1.245): the Linux startup crash on glibc 2.44 distributions (Arch, CachyOS, Fedora Rawhide) is fixed (surface 9 — macOS/Linux is the plugin's stated floor; this closes a gap on newer glibc, nothing to do). Checked and cleared: `--agents` now erroring on invalid JSON/agent definitions (surface 5) is about the CLI `--agents` flag, a different mechanism from this plugin's `agents/*.md` frontmatter files; plugin dependencies declared with a `marketplace` field failing to resolve under `--plugin-dir` (surface 6) needs a `dependencies` field this plugin's manifest doesn't have; the cross-session messaging inbox socket now closing connections that send no complete line within 30 seconds (surface 5) is aimed at raw socket posters, and no script in this repo talks to that socket directly — messaging goes through the `SendMessage` tool; `modelPicker`/`promptCacheTtl`/`subagentPromptCacheTtl`/`modelPricing` settings and the model/effort-per-subagent display in `/tasks` (surface 5) are additive, interactive-only features nothing here reads or depends on; remote MCP reconnect and MCP desktop sign-in fixes — no `.mcp.json` or inline MCP anywhere in the repo (surfaces 2, 8, previously established). 88 of roughly 100 combined entries irrelevant (`/usage` Loops breakdown, keyless Console sign-in, `/status` managed-settings and GitHub-web lines, `/model`/`/resume`/`/clear`/Ctrl+R/Ctrl+[ picker and history fixes, spellcheck, workload-identity-federation CI, hook `if`-condition substitution fix (no hooks configured here), `/reload-plugins` LSP-tool cleanup, `--teleport`, `/web-setup` GitHub-CLI auth, Claude in Chrome auto-update reconnect, VSCode Focus-view/history/telemetry fixes, startup-time/binary-size/memory-footprint improvements, `/login` over SSH, `/effort` error text, `/loop` wake-up folding, `/model` picker Sonnet-5 pricing update, macOS Finder access-dialog gate for computer use, Bedrock/Vertex/Foundry immediate `/model`/`/fast`/`/effort` application, `claude remote-control` recovery and Team/Enterprise stuck-session fixes, Remote-Control-held-elsewhere resume notice). |
| 2026-08-24 | 2.1.239 → 2.1.241 | **Nothing to judge: both new entries publish no detail.** 2.1.241 and 2.1.240 each carry the single line "Bug fixes and reliability improvements" and nothing else — identical in the docs changelog and in the upstream `CHANGELOG.md`, checked against both. No mechanism is named, so no entry can be tied to a surface, and no verdict beyond "unassessable" is honest here: not flagged, but not cleared either. If a surface regression shows up on 2.1.240/241, these two lines are where it hides. Not exercised on this host yet — the installed CLI is 2.1.237, two releases behind the baseline, so the plugin currently runs on a version whose entries were all reviewed in the rows below. Re-read this window if the entries are backfilled with detail later. |
| 2026-08-24 | 2.1.238 → 2.1.239 | **One behavior shift to verify, on the title-based addressing the whole foreman protocol is built on.** BEHAVIOR (low): "Fixed custom session titles disappearing from `/resume` after more than ~64 KB of conversation was written following the rename" (surface 5) — `foreman.md:39-45` builds the whole protocol on the session title as "the one address that works" (`set_session_title`, then exact-match lookup via `list_sessions`/`ListAgents`), and phase/repair sessions run up to 25 turns, which can plausibly exceed 64 KB. Undetermined whether "disappearing from `/resume`" means only the resume-picker's display or the actual title `ListAgents`/`list_sessions` return — if the latter, a long-running phase chat's address silently reverts mid-run, which is exactly the "dead channel" case `foreman.md:108-112` says must be *declared*, not discovered. Verify a long phase or foreman chat still answers to its title after >64 KB of transcript. POSITIVE: the Linux sandbox no longer makes a nonexistent `.git/config.worktree` unreadable, which broke every sandboxed git command in repos with `extensions.worktreeConfig` set (surface 8 — this repo's own git config carries `extensions.worktreeConfig=true`, verified, and the plan checkout is a linked worktree; on a Linux sandbox every git command the workflow issues was broken until this release, now fixed), `/resume` in all-projects mode no longer tells you to `cd` into a deleted directory such as a removed worktree, and now resumes in the current directory instead (surface 8 — matches this plugin's own worktree-removal-after-finalize shape), starting Claude Code from a directory that no longer exists no longer raw-crashes and now prints a clear message (surface 8, same family), `ListAgents` now tells a session its own name and `SendMessage` to your own name says so instead of "no agent named …" (surface 5 — softens, for the CLI channel, the limitation `foreman.md:39-40` states as settled, "a session cannot read its own id"; convenience only, nothing in the protocol depended on that limitation holding), `/goal` repeat check-ins on long-running background work now back off (30 min, then 1 h, then every 2 h) instead of repeating every 30 minutes (surfaces 2, 5 — continues the thread opened at 2.1.234 and sharpened at 2.1.237; this is the fix direction, fewer injected turns per stall, not more). Checked and cleared: the 1.1× data-residency cost premium now included in `--max-budget-usd` estimates (surface 3 names this exact flag, `run-workflow.sh:382`, but the premium only applies to data-residency workspaces and does not change the flag's syntax or failure mode); Windows cross-session messaging reaching parity with macOS/Linux (surface 9's macOS/Linux/WSL confinement is about the bash/python3 host toolchain, untouched); `ListAgents`/`/list-agents` now listing live "teammates" (surface 5 — this plugin spawns Agent-tool subagents and cross-session peers, never agent-team teammates); sessions whose title starts with `/` no longer unaddressable (surface 5 — no title this protocol assigns, `wf:<slug>:foreman`/`:phase-N`/`:repair-N`, starts with `/`); runaway session-title syncing to Remote Control now deduplicated (surface 5 — a Remote Control sync bug, not the plain CLI `list_sessions`/`ListAgents` path this protocol reads); `CLAUDE_CODE_RETRY_WATCHDOG` failing fast on spend-limit errors (not set anywhere in this repo); hooks now surviving a deleted session cwd (no `PreToolUse`/`PostToolUse` hook is configured anywhere in this repo); `claudeMdExcludes` symlink fix (no such config here); `.worktreeinclude` `**/`-prefix fix (no `.worktreeinclude` file in this repo); BOM-ignored skill/agent files (no BOM in any `.md` here); marketplace `pluginRoot` fix (this repo's manifest is a plain path source with no `pluginRoot`); `claude -c`/resume picking up a wrong directory differing only by punctuation, and `/goal` restoring itself across `--resume` (neither launcher ever uses `--resume`/`-c`, both always launch fresh `claude -p` sessions). 53 of 59 entries irrelevant (Bedrock/proxy streaming and SSO-hang fixes, Alpine/musl add-ons, JetBrains Edit/Write pause, Esc-with-queued-prompt race, WebFetch in-memory cache duration, remote MCP 5xx reconnect, MCP elicitation form clipping, `dark-ansi` fullscreen contrast, fullscreen-prompt three-launch cap, `/insights` tag echo, mouse-report terminal garbling, theme badge-color override, OpenTelemetry trace fragmentation, vim-mode Escape, `selection:copy` with Shift+Arrow, `/voice` tip, shell-mode Tab completion, fullscreen focus-click prompt, fullscreen panel overlap, `/workflows` dialog overflow, pasted-text placeholder corruption on delete keys, masked-input paste/history leak, Ctrl+Backspace in search boxes, org-policy-rejected request resend, post-compaction skill-arg reminder, file-path truncation, remote-session keep-alives, `keybindingFlavor` Bash word keys, Claude in Chrome tab-group `/clear`, mobile image upload paths, Claude Code web Bash-through-proxy, Remote Control not-enabled message, VSCode usage-banner layout, `/claude-api upgrade` migrator, cloud-session plugin sync naming, fullscreen-renderer offer on Bedrock/Vertex/Foundry, usage-limit message reset wording). |
| 2026-08-21 | 2.1.237 → 2.1.238 | **Three behavior shifts, all low, one of them on a field-tested claim `foreman.md` states as settled.** BEHAVIOR (low): "Fixed `ListAgents`/`SendMessage` reporting 'Remote Control is not connected' in sessions run by `claude remote-control` (server mode) or Desktop/IDE hosts; they now list and reach Remote Control peers" (surface 5) — `foreman.md`'s *Channel floors* is the plugin's single source for messaging floors and it asserts, as field-tested on 2.1.226, that "CLI and desktop are separate worlds" and that "`SendMessage` does not resolve desktop sessions or their titles (field-tested: id, title and `ListAgents` names all unreachable)" (`refs/foreman.md:106-131`, `:281-283`). This release widens what the CLI tools reach from a Desktop/IDE-hosted chat — to Remote Control peers, which is not the same population as desktop-app chats, so the claim is not falsified, but the boundary it draws moved and the whole protocol reads reachability off it. Re-test the branch declaration from a desktop-hosted chat before trusting the "neither world" line; if the CLI branch now works where the ref says it cannot, the floor for it is 2.1.238, not 2.1.224. BEHAVIOR (low): "Fixed unbounded memory growth in long interactive sessions: subagent tool results are now released once they leave the recent display window" (surface 5) — the interactive path spawns `phase-verifier`, `ui-judge` and `report-judge` and every skill consumes their findings in the same step (MECHANICAL → fix, JUDGMENT → `> Review:` in the plan, `execute-phase-agent/SKILL.md:79`, `repair-phase/SKILL.md:65`), so the findings are on disk before the display window can matter; the exposure is a long `/run-workflow` chat holding a Monitor loop across many phases, where whether "subagent tool results" means the Agent-tool result or the tools the subagent called decides whether findings can be released before they are written down. Verify the `> Review:` write lands in the same turn as the spawn. BEHAVIOR (low): "Improved Bash tool permission checking for zsh-specific syntax in shell conditionals" (surfaces 7, 9) — this host runs zsh and the checker's verdict on a conditional-bearing command decides whether an unattended phase proceeds or stalls on a prompt; the launchers themselves carry no `[[ ]]` (their only `[[` hits are `[[:space:]]` classes inside grep patterns) and the one conditional any skill prescribes is `if [ "$COUNT" -gt "$SEEN" ]` in the interactive `run-workflow` Monitor loop (`run-workflow/SKILL.md:65`), but every `--permission-mode auto` sub-session issues model-authored Bash, which is where a reclassification would show up. Fix-direction entry, so the likely effect is fewer wrong verdicts; unverified either way. POSITIVE: permission prompts no longer drop the "don't ask again" option when the project path did not fit the terminal width (surface 4 — this partly answers the open 2.1.235 item below: some of the narrowing of the inherited allow-list in `.claude/settings.local.json` was this bug, and grants the autonomous path relies on can now be recorded again), a session refusing inbound cross-session messages (`crossSessionInbound: "refuse"`) now reports "refused" to the sender instead of a silent success (surface 5 — `foreman.md:285-289` already treats a refused delivery as a skip-in-silence, and a false success was the worse failure: the child believed its report arrived), an inbox dropping messages on rate limit or a full queue now tells the sender (surface 5, same clause), `ListAgents`/`SendMessage` no longer expose the idle worker pre-warmed for the next background session (surface 5 — the foreman lookup resolves its target by exact title/name off that listing, so a phantom entry was a false target), leftover `/tmp/claude-*-cwd` files no longer accumulate when a Bash command is killed, times out or is interrupted (surface 9 — killed unattended runs are exactly that case, and the run log lives under the same `${TMPDIR:-/tmp}`). Checked and cleared: plugin-marketplace `headersHelper` on url marketplaces and catalog entries, and the new `[y/N]` confirmation on `claude plugin install/update` (surface 6) — this repo's manifest is a path source, no config anywhere in the repo carries `headersHelper` or `mcpServers`, and the only `claude plugin install/update` lines are human-facing instructions in `README.md:243-280` and an echoed hint in `plugins/wf/install.sh:88-89`, never a script call that a prompt could hang (`-y` exists if that changes); "MCP `headersHelper` in a project `.mcp.json`, and inline MCP servers in project or `--add-dir` agent files, now require that folder's trust dialog to have been accepted (also under `claude -p`)" (surfaces 2, 8) — no `.mcp.json` and no inline MCP anywhere in the repo, and neither launcher passes `--add-dir`, so the gate never fires; noted only as a second precedent of a trust dialog reaching headless sessions while the 2.1.232 worktree-trust question is still unverified; the worktree-isolation Bash-refusal message fix (surface 8 excludes native isolation by its own wording — the plan checkout is a plugin-managed worktree run as a normal session); plugin output styles no longer drifting to the default voice mid-session — this plugin ships none. 27 of 39 entries irrelevant (`keybindingFlavor` readline setting, two `claude self-hosted-runner` flags and the slow-poll removal fix, `CLAUDE_CODE_ENABLE_PROMPT_SUGGESTION` near-limit fix, held-Backspace on Ctrl+H terminals, permission-diff wide-character wrapping, Ctrl+Z bracketed-paste, stdio MCP `server/discover` before `initialize`, proxy refusal naming, `/model`-`/effort` cache-miss warning, ten Remote Control fixes, faster macOS startup, deferred update check, bundled `claude-api` skill refresh, fullscreen Ctrl+L/Cmd+K repaint and the removed double-press `/clear`, `claude mcp list` disabled-server display, MCP helper credential-env isolation). |
| 2026-08-20 | 2.1.235 → 2.1.237 | **One behavior shift — an escalation of the already-tracked 2.1.234 `/goal` check-in concern.** BEHAVIOR: "`/goal`: an idle session whose goal is parked behind long-running background work now checks in automatically after 30 minutes (then 1h, 2h) instead of waiting for you to return" (surfaces 2, 5) — sharpens the 2.1.234 entry logged below: every phase session runs as `claude -p '/goal <contract> … Stop after 25 turns.'` (`run-workflow.sh:175-176`) and spawns `phase-verifier`/`ui-judge`/`report-judge`, which run in the background by default since 2.1.232; where one check-in used to risk injecting an unplanned turn into the 25-turn cap, a parked goal can now be checked in on up to three times (30m, 1h, 2h) before it returns. Still unverified — the 2.1.234 concern stands, now with a higher ceiling on how many extra turns a single stall can add. POSITIVE: `SendMessage` no longer silently drops a message whose closing tag was malformed (surface 5 — `common.md:512` already treats a refused delivery as a silent skip, now less likely to need it), a rapid `SendMessage` burst is now refused up front instead of reported sent while dropped (surface 5, same file — moot in volume terms since the foreman relay sends single header lines far under any cap), auto mode's git status check can no longer be fooled by `status.showUntrackedFiles=no` into reporting a clean tree (surface 7 — neither this repo nor the user's git config sets it, so behavior here is unchanged but the fix is real), a slash-command typo or unavailable command in a headless session now reports instead of running the closest fuzzy match (surface 2 — guards a bug that would silently launch the wrong skill from `agent-session.sh`/`run-workflow.sh`'s `/$PLUGIN_NAME:$SKILL` construction; both already build the name correctly), background sessions and local MCP logs no longer break after the directory a session had switched into was removed (surface 8 — worktree checkouts under `.claude/worktrees/<slug>` are exactly this shape, though `finalize-workflow`'s worktree removal already runs only once subagents have exited). Checked and cleared: macOS sandbox wildcard read-deny precedence for files like `.env` — no listed surface covers arbitrary file protection and no script reads such files; the Fable 5 usage-credits auto-fallback-on-timeout fix is scoped to Remote Control, not the headless `claude -p --model fable` this repo uses (`run-workflow.sh:446-449`); auto mode's Bedrock/Vertex/Foundry/telemetry-disabled classifier parity — this session runs the default `ANTHROPIC_BASE_URL=https://api.anthropic.com` with telemetry not disabled, so none of the three conditions apply; SIGTERM in print mode no longer recording a synthetic interrupted-turn note — the launcher never sends SIGTERM to its sub-sessions and reads only `$?` and plan-file state, never transcript text; "Monitor allow rules set aside during auto mode" — Monitor sits only in the interactive `run-workflow` skill's `allowed-tools`, never in `execute-phase-agent`/`repair-phase-agent`, and the interactive skill never runs under `--permission-mode auto`; the SDK/VS Code skills-hot-reload-after-deleted-cwd fix does not cover this plugin's headless CLI sessions; the WSL `powershell.exe` unhandled-rejection fix is Windows-only. 24 of 35 entries irrelevant (`ANTHROPIC_DEFAULT_MODEL` env var — scripts always pass an explicit `--model`, fullscreen-renderer/tab-title/blank-band/repaint fixes, `/model` picker height and highlight, managed-settings approval-prompt timing, unclear cloud-environments-list error, spinner-tips/guest-pass fix, self-hosted-runner resume race, Clawd mascot rendering, session-recap length cap, background session-counter write, `/usage` spend row for Team/Enterprise, Remote Control offline detection, session-title-chip and footer alignment, VSCode screen-reader support, prompt-caching fix for custom base URLs, the new "Concise" output style). |
| 2026-08-12 | 2.1.221 → 2.1.228 | No breaking changes. POSITIVE: 2.1.228 symlinked-dev-checkout cache fix, 2.1.225 headless 401 + auto-mode fixes, 2.1.224 multi-project install records + subagent cap removal. All other entries irrelevant. |
| 2026-08-19 | 2.1.234 → 2.1.235 | **One behavior shift to verify, on the permission grants the autonomous run inherits.** BEHAVIOR (low): "Improved permission dialogs: display text and 'don't ask again' options now always match what a grant would cover, and 'don't ask again' is withheld when contents cannot be fully displayed" (surface 4) — `.claude/settings.local.json` carries only `permissions.allow`, and those rules are what an interactive "don't ask again" writes; both launchers copy that file into the plan checkout (`agent-session.sh:73-74`, `run-workflow.sh:89-90`) so sub-sessions inherit it. A grant that now covers exactly what the dialog displayed, or that is withheld entirely when the contents cannot be shown, yields a narrower inherited allow-list than the same interactive answers produced before — and the narrowing surfaces as an unattended phase stalling on a prompt with nobody in front of it, not as an error. Verify after the next interactive run that the rules the autonomous path relies on are still recorded. POSITIVE: Shift+Tab in the permission prompt's comment field no longer approves the edit and grants session-wide edit permission (surfaces 4, 7 — the same accidental broad grant would have landed in the copied settings file), the Agent tool no longer advertises a general-purpose default where that agent is unavailable and an omitted `subagent_type` now gets a clear error listing the available agents (surface 5 — `execute-phase-agent/SKILL.md:79` and `execute-phase/SKILL.md:72` both document "fallback: a general-purpose subagent", which now fails legibly instead of obscurely where it does not exist), `SendMessage` refuses messages too large for cross-session delivery up front instead of silently dropping them (surface 5 — the foreman protocol sends single header lines (`common.md:421-430`), far under any cap, and `common.md:512` already treats a refused delivery as a silent skip), embedded `grep` in native macOS/Linux builds fails fast on pathological patterns instead of exhausting memory and prints correct context for `-m N` with `-A/-C` (surface 9 — no script passes those flags; the skills reach grep through the Grep tool). 14 of 19 entries irrelevant (`spellcheck` prompt-input setting, language-server prompt-cache invalidation, nested markdown list alignment and hanging indents, prompt-input highlight offsets, notebook cell approval dialog content, slash-command HTML entities, update-installed restart notice, `ctrl+t` task list starting collapsed, cloud-session memory/CPU, context-limit auto-compact message, Vim NORMAL-mode preservation, dialog arrow-key selection, Remote Control `claude rc` gateway check, VSCode tab focus). |
| 2026-08-18 | 2.1.233 → 2.1.234 | **Five behavior shifts to verify, three of them on the `/goal` guard and the usage-limit path that every autonomous run depends on.** BEHAVIOR: "`/goal`: when background tasks keep a goal waiting for 30+ minutes, Claude now checks in on them instead of waiting indefinitely (set `CLAUDE_CODE_GOAL_CHECKIN_MINUTES=0` to opt out)" (surfaces 2, 5) — every phase session runs as `claude -p '/goal <contract> … Stop after 25 turns.'` (`run-workflow.sh:162-176`), and the skills spawn `phase-verifier`/`ui-judge`/`report-judge`, which since 2.1.232 may run in the background; a check-in injects unplanned turns into a contract whose runaway cap is counted *in turns*. Verify the cap still holds, or set the env var to 0 in the launcher. BEHAVIOR: "Claude Code now continues your session automatically when a claude.ai usage limit resets; turn it off in `/config`" (surfaces 2, 3) — if this reaches headless `claude -p`, a phase session that hits the limit waits for the reset instead of exiting, so the launcher's non-zero-exit paths (the fable→opus repair fallback at `run-workflow.sh:426`, and the parent Monitor that terminates on the launcher's final line) block for as long as the reset takes; the opt-out is an interactive `/config` toggle with no CLI flag named. BEHAVIOR: "`/goal` now clears itself with a notice when a turn dies on an unrecoverable error (e.g. revoked auth, an exhausted credit balance, or a context overflow) instead of staying armed" (surface 2) — the goal guard is what stops a phase session declaring itself done before the plan shows the outcome, and a 25-turn phase can plausibly overflow context; verify the launcher still detects a no-progress session from plan state once the goal disarms itself. BEHAVIOR (low): "Removed the 'Default teammate model' setting from `/config`; agent-team teammates now use the leader's model unless the spawn names one" (surface 5) — all three agents declare `model: opus` in frontmatter; if only an explicit model *on the spawn* counts as "naming one", a verifier spawned from a `fable` phase session silently downgrades, the exact failure surface 5 exists to catch. "agent-team teammates" may name a different feature than Agent-tool subagents; not tested here. BEHAVIOR (low): "Fixed trust prompts omitting the repository-wide scope warning when the directory was first seen before the repository existed there" (surface 8) — not the plan checkout's case (its directory is created fresh by `git worktree add`), but trust-prompt scoping moved again while the 2.1.232 nested-repo trust question is still unverified; check the two together. POSITIVE: auto mode no longer re-checks and denies sandboxed commands' network access after compaction (surface 7 — long unattended phase sessions are exactly that case), session-scoped permission answers no longer dropped when answering background subagent prompts (surfaces 5, 7 — the 2.1.232 background-spawn default), the fullscreen-renderer prompt no longer restarts a session without its permission mode/model/effort flags and `/tui` now declines to switch rather than dropping launch tool rules (surface 3), `strictKnownMarketplaces` no longer accepts SCP-style git sources whose host differs from git's (surface 6 — this repo's manifest is a `./plugins/phased-workflow` path source, unaffected), between-turn background notifications now arrive in `<system-reminder>` tags like mid-turn ones (surface 5). Checked and cleared: removal of the "Allowed by auto mode classifier" line under every Agent call (surfaces 5, 7) — the scripts `tee` session output to `$PLAN_DIR/log/*.txt` and never grep it, every parse runs over the plan file or `next-phase.py`; `claude setup-token` now rejecting extra arguments (surface 3) — no script calls that subcommand and the strictness is scoped to it; the NT-namespace (`\??\`) path hardening and the read-only `~/.claude.json` startup fix are Windows-only (surface 9); `CLAUDE_CODE_PROJECT_DIR_NAME` is opt-in and names the per-project transcript directory only, not settings resolution (surface 4). 40 of 51 entries irrelevant (Remote Control sync/upload/account-switch, permission-preview relay and credential masking, MCP diagnostics secrets, GitLab MR badge, `selection:clear` keybinding, transcript/markdown/`---`/Unicode rendering, queued-`!` and mid-turn dialog fixes, SendMessage/ListAgents naming and list-truncation notices, session titles, `claude-api` skill context cost, `/permissions` and `/add-dir` mid-turn, Mantle admin-pin probe, non-streaming-fallback crash, repo-host userinfo detection). |
| 2026-08-15 | 2.1.232 → 2.1.233 | No breaking changes, no behavior shifts. POSITIVE: skill/command argument substitution fixed to stop re-expanding argument values as template markers (surface 1, e.g. `$1`/`$ARGUMENTS` in `issue`/`pull-request`/`scope-workflow` SKILL.md), `claude plugin validate` now checks a bare `.claude/skills` directory and reports SKILL.md frontmatter parse failures (surfaces 1, 6). Checked and cleared: Todo/task-tracking tools (TaskCreate/Update/List, TodoWrite) disabled by default on Fable 5/Sonnet 5/Opus 4.8/Mythos 5+ — no skill, agent or script in this plugin uses those tools, phase state is tracked entirely in `.phased/` plan files and `next-phase.py` (no listed surface); bundled skill-alias shadowing fix (`/checkup`, `/review`) — no plugin skill name collides; reverted 2.1.232 Bash permission changes for `< file` redirection and Windows Cygwin symlinks — no stdin redirection in any script, and the Windows-only fixes (NTLM path validation, auto-mode `cd &&` regression) fall outside surface 9's macOS/Linux/WSL scope. 15 of 20 entries irrelevant (GitLab MR `--worktree` display, apps-gateway identity/error-forwarding, Bash memory cgroups, WebFetch cache TTL, cloud-session/MCP-v2/idle-CPU/notification-hook fixes, self-hosted-runner startup, screen-reader mode, unrecognized-model stderr diagnostic, GitHub app setup tip). |
| 2026-08-14 | 2.1.231 → 2.1.232 | **Two behavior shifts to verify.** BEHAVIOR: "non-teammate agent spawns in interactive sessions now run in the background by default" (surface 5) — the skills spawn `phase-verifier`, `ui-judge` and `report-judge` and consume their findings in the same step (`report-judge` explicitly "before showing"); no skill passes `run_in_background: false`, so on the interactive path the spawn may return an agent name instead of findings. Headless `claude -p` sub-sessions are outside the entry's stated scope. BEHAVIOR: "Fixed nested git repositories inheriting trust from a parent directory; each repository now requires its own trust confirmation" (surface 8) — the plan checkout is a linked worktree at `$REPO_ROOT/.claude/worktrees/<slug>`, nested under the already-trusted repo root, and `agent-session.sh`/`run-workflow.sh` `cd` into it and launch headless; if a `.git`-file worktree is classified as its own repository, the first run for a new plan meets a trust prompt nothing can answer. Not tested here. BEHAVIOR (low): Fable 5 advisor consent "set up through `/model fable`" (surface 3) — a usage-credits consent step cannot be performed headless, though `run-workflow.sh` already falls back to opus when a fable session exits non-zero. POSITIVE: plugin-marketplace startup race on `known_marketplaces.json` fixed, `/plugin install plugin@marketplace` now refreshes first, GitLab marketplace sources and the `additionalMarketplaces`/`allowedMarketplaces` aliases are additive (surface 6). Checked and cleared: Bash `< file` redirections now permission-checked — no stdin file redirection in any script or skill (surfaces 7, 9); `sandbox.ripgrep` no longer honored from project settings — `.claude/settings.local.json` carries only `permissions.allow`, but note the precedent, the scripts copy project-scope settings into the checkout (surface 4); the PowerShell and Git Bash permission-bypass fixes are Windows-only (surface 9). 42 of 49 entries irrelevant. |
| 2026-08-13 | 2.1.228 → 2.1.231 | No breaking changes, no behavior shifts. POSITIVE: 2.1.229 auto mode no longer fails every tool call when `CLAUDE_CODE_ATTRIBUTION_HEADER` is disabled (surface 7), stray `claude plugin` liveness file no longer blocks cleanup of outdated plugin versions (surface 6), new marketplace `command` sources as an alternative to symlinked dev installs — additive, path sources like this repo's manifest unaffected (surface 6), `/model` no longer rejects Opus/Sonnet 1M behind a custom `ANTHROPIC_BASE_URL` (surface 3), oversized conversations now fail once with a clear message instead of looping on compaction (surface 2). Checked and cleared: the `/commit-push-pr` dangerous-flag de-auto-approval does not reach this plugin (no `--force`/`--amend`/`--no-verify` in any script or skill), and the fail-closed sandbox domain enforcement finds no network config in `.claude/settings.local.json`. 27 of 32 entries irrelevant. |
